const express = require('express');
const app = express();

app.use(express.json());

// ข้อมูลจำลองสินค้าจากชุมชน (ในอนาคตจะเปลี่ยนเป็นดึงจาก Database)
const products = [
    { id: 1, name: "ข้าวหอมมะลิแท้", origin: "ทุ่งกุลาร้องไห้", price: 250, unit: "5kg" },
    { id: 2, name: "น้ำผึ้งป่าเดือนห้า", origin: "ชัยภูมิ", price: 450, unit: "750ml" },
    { id: 3, name: "ผ้าทอมือลายโบราณ", origin: "ลำพูน", price: 1500, unit: "ผืน" }
];

// หน้าแรกเมื่อมีคนเข้ามาที่ระบบ
app.get('/', (req, res) => {
    res.send(`
        <body style="font-family: sans-serif; text-align: center; padding: 50px;">
            <h1>🌳 ยินดีต้อนรับสู่ Rakrao (รากเรา)</h1>
            <p>ระบบ Backend สำหรับสินค้าชุมชนรันสำเร็จแล้ว!</p>
            <a href="/api/products" style="text-decoration: none; color: green; font-weight: bold;">ดูรายการสินค้าทั้งหมด (API)</a>
        </body>
    `);
});

// หน้า API สำหรับส่งข้อมูลสินค้าไปให้หน้าเว็บแสดงผล
app.get('/api/products', (req, res) => {
    res.json(products);
});

// ตั้งค่า Port 3000
const PORT = 3000;
app.listen(PORT, () => {
    console.log(`Rakrao system is running on http://localhost:${PORT}`);
});
